DJANGO_SECRET_KEY=-!q$%x@i-24nf$4%iir*!)fclw9jzrqcl2tubhs!(0aor^(kbu
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost
DJANGO_CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

PAYMENT_PROVIDER=razorpay
RAZORPAY_KEY_ID=your_key
RAZORPAY_KEY_SECRET=your_secret

# ── Email / SMTP (leave blank for console backend in DEBUG mode) ──────────────
# In production, set these to your transactional email provider credentials.
# Supported: SendGrid, Mailgun, Resend, AWS SES, or any SMTP service.
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=true
EMAIL_USE_SSL=false
DEFAULT_FROM_EMAIL=Udaan Studio <hello@udaanstudio.com>

# ── Frontend URL (where React dev server / production site runs) ───────────────
FRONTEND_URL=http://localhost:5173

# ── Google OAuth2 (Google Sign-In for CASA) ─────────────────────────────────
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com